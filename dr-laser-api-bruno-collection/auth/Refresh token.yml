info:
  name: Refresh token
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/refresh
  body:
    type: json
    data: |-
      {
        "refresh_token": "GrgrzP75tBThJRhHubRTrtV6"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
